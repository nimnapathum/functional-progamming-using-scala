object question_3_02{
    def filterAboveFive(list: List[String]):List[String]={
        for (element <- list){
            if(element.length() > 5){

            }
        }
    }
    def main(args: Array[String]):Unit={

    }
}